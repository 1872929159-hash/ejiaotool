# 多人协作上手指南

> 本文档写给参与「东阿阿胶·京东营销合规检测工具」完善工作的同事。
> 你不需要懂编程，只需要会「对 WorkBuddy 说话」和「点几下按钮」。

---

## 一、这个项目是什么

一个网页工具：粘贴营销文案 → 自动检测违禁词/敏感词 → 还能用 AI 复核并给出合规改写建议。词库 763 条，覆盖京东/抖音/小红书等多平台。

核心源码只有一个文件：`tools/合规检测工具.html`（约 953KB，词库内嵌在里面）。

---

## 二、协作模式（一句话版）

> 你对 WorkBuddy 说要改什么 → WorkBuddy 帮你改代码 → 你提交到自己的分支 → 负责人合并 → 网站自动更新 → 大家刷新就能看到。

**"实时"体现在哪**：任何人改完合并后，大约 30-90 秒，公网地址就更新了，所有人刷新浏览器即可看到彼此的改动。不需要等谁"发布"。

---

## 三、一次性环境准备（每人只做一次）

### 1. 装三个东西

| 软件 | 下载地址 | 用途 |
|------|---------|------|
| **Node.js**（选 LTS 版） | https://nodejs.org | 运行本地预览服务 |
| **WorkBuddy 桌面版** | （已安装的可跳过） | 你指挥它改代码 |
| **GitHub Desktop** | https://desktop.github.com | 图形化管理代码，比敲命令简单 |

### 2. 拿到代码

1. 让负责人把你加进 GitHub 仓库（会给你一个仓库网址）。
2. 打开 GitHub Desktop → 登录 GitHub 账号 → Clone 仓库到本地（建议放在 `D:\` 下，路径别带中文）。

### 3. 验证本地能跑

进到仓库目录，**双击 `start.bat`** → 浏览器自动打开 `http://localhost:3456` → 能看到检测工具页面就说明环境 OK。

> 不想装 Node.js？也可以，直接靠公网地址看效果就行（见下文）。但本地预览改代码时更快，不用每次都等部署。

---

## 四、日常协作流程（每次改东西都走这 5 步）

```
① 同步最新代码     →  GitHub Desktop 里点 "Fetch origin" + "Pull"
② 开自己的分支     →  对 WorkBuddy 说："从 main 开一个分支叫 feature/我的名字-改了啥"
③ 改代码           →  对 WorkBuddy 说需求，它在本地帮你改 tools/合规检测工具.html
④ 本地看效果       →  双击 start.bat，刷新 localhost:3456
⑤ 提交并请求合并   →  对 WorkBuddy 说："提交并推送"，然后去 GitHub 网页发 Pull Request
```

负责人合并后，等 30-90 秒，刷新公网地址就能看到新效果。

---

## 五、公网预览地址

（部署后由负责人填在这里）

> 公网地址：`________________`
> 合并到 main 分支后约 30-90 秒自动更新，所有人刷新即见。

---

## 六、分支约定（很重要，避免冲突）

- **main 分支**：受保护，不能直接改，只能通过 Pull Request 合并。合并后自动更新公网。
- **你的分支**：每次改一个功能就开一个，命名 `feature/你的名字-简短功能`，例如 `feature/张三-按钮配色`。
- **一次只改一个功能**：改完合并，再开下一个。别同时挂着一堆分支。
- **开改前一定先 Pull**：拿到最新代码再改，否则合并时必冲突。

---

## 七、单文件分区约定（降低撞车概率）

整个网站就一个文件 `tools/合规检测工具.html`，多人同时改容易撞。约定分成 5 个区，每次只动一个区，并在 PR 标题注明：

| 区 | 内容 | 例子 |
|----|------|------|
| **A 区** | 样式 / 颜色 / 布局 | 改按钮颜色、调字体 |
| **B 区** | 词库数据（763 条，一般别动） | 加新违禁词 |
| **C 区** | 检测逻辑 | 调检测规则 |
| **D 区** | AI 复核逻辑 | 改 AI 提示词 |
| **E 区** | UI 文案 / 标题 / 按钮文字 | 改提示语 |

PR 标题示例：`[A区] 检测按钮改成暗红色`。两个人同时改不同区，基本不冲突；同时改同区，后合并的人让 WorkBuddy 帮忙处理冲突。

---

## 八、给 WorkBuddy 的话术示例

| 你想要的 | 对 WorkBuddy 说 |
|---------|----------------|
| 改配色 | "把检测按钮的颜色改成 #993556，清空按钮改成灰色" |
| 改文案 | "把顶部标题从'京东营销合规'改成'多平台营销合规检测'" |
| 加功能 | "在检测结果下面加一个'导出报告'按钮，点击下载 txt" |
| 提交代码 | "把当前改动提交，消息写'调整按钮配色'，推到我的 feature 分支" |
| 同步代码 | "先 pull 一下 main 分支的最新代码" |

---

## 九、常见问题

**Q：我双击 start.bat 没反应 / 报错？**
A：多半是没装 Node.js，或装了没重启。装 LTS 版后重试。实在不行，直接用公网地址看效果。

**Q：合并时提示冲突怎么办？**
A：别慌，对 WorkBuddy 说："帮我解决合并冲突"，它会帮你处理。或者让负责人帮忙。

**Q：我改完怎么看不到公网更新？**
A：① 确认你的改动已合并到 main（不是只在你自己分支）；② 等 30-90 秒；③ 浏览器强制刷新（Ctrl+F5）。

**Q：我不小心改坏了怎么办？**
A：只要没合并到 main 就没事，删掉自己的分支重来。main 是受保护的，坏代码进不去。

**Q：词库要加新词怎么办？**
A：词库是 B 区，改动较大，建议单独开分支、单独 PR，并和负责人确认。

---

## 十、负责人备忘

- [ ] 创建 GitHub 私有仓库，把同事加为协作者
- [ ] 把公网地址填到本文档第五节
- [ ] main 分支设为受保护，强制走 Pull Request
- [ ] 把本文档地址发给所有同事
