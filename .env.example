# 东阿阿胶京东营销合规检测工具 —— 环境变量示例
#
# 本文件仅作说明，程序不会自动读取。请按下方方式在运行环境中设置。
#
# ============================================================
# DEEPSEEK_API_KEY —— DeepSeek 大模型 API Key（AI 智能复核功能所需）
# ============================================================
#
# 说明：
#   - 词库检测（67 条违规词）为纯本地运行，不需要任何 Key，也不需要联网。
#   - 仅「AI 智能复核」功能需要此 Key。
#   - 若不设置此环境变量，用户仍可在页面右上角「⚙ API 设置」中填入自己的 Key，
#     该 Key 只保存在浏览器 localStorage，不会上传到任何服务器。
#
# 获取方式：
#   登录 https://platform.deepseek.com → API Keys → 创建 API Key
#
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here


# ============================================================
# 各环境设置方式
# ============================================================
#
# Windows (CMD)：
#   set DEEPSEEK_API_KEY=sk-xxxx
#   node proxy-server.js
#
# Windows (PowerShell)：
#   $env:DEEPSEEK_API_KEY = "sk-xxxx"
#   node proxy-server.js
#
# macOS / Linux：
#   export DEEPSEEK_API_KEY=sk-xxxx
#   node proxy-server.js
#
# EdgeOne Pages（云端部署）：
#   控制台 → 项目设置 → 环境变量 → 新增 DEEPSEEK_API_KEY
#   配置后需重新部署一次才生效。
#
#
# ============================================================
# 安全提示
# ============================================================
#
# 若将本工具部署到公网并使用服务端托管的 Key（访客无需自备 Key 即可体验），
# 建议单独创建一张仅充少量额度的 Key，与主账号 Key 隔离，
# 避免公网暴露导致额度被恶意消耗。
